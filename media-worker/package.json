{
  "name": "snape-media-worker",
  "private": true,
  "description": "Standalone Cloudflare Worker that proxies video bytes (off the Next.js Pages app to avoid Error 1102 during playback).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "wrangler": "^4.78.0"
  }
}
